# Example environment variables for local development
# Copy this file to .env (but DO NOT commit .env) and fill with real values.

# Google credentials
# Option A (recommended for local): path to service account JSON
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option B: raw JSON content (single line) assigned to datecKeyCredentials
# Use: datecKeyCredentials=$(jq -c . /path/to/service-account.json)
# or datecKeyCredentials={...}

datecKeyCredentials={...}

# OpenAI
API_OPENAI_KEY=

# LlamaParse
LLAMAPARSE_API_KEY=

# SAP configuration (credentials + optional custom endpoints)
SAP_USERNAME=
SAP_PASSWORD=
SAP_SUPPLIER_URL=https://my408830-api.s4hana.cloud.sap/sap/opu/odata/sap/API_BUSINESS_PARTNER/A_Supplier
SAP_PURCHASE_ORDER_URL=https://my408830-api.s4hana.cloud.sap/sap/opu/odata/sap/API_PURCHASEORDER_PROCESS_SRV/A_PurchaseOrder
SAP_INVOICE_POST_URL=https://my408830-api.s4hana.cloud.sap/sap/opu/odata/sap/API_SUPPLIERINVOICE_PROCESS_SRV/A_SupplierInvoice
SAP_MATERIAL_DOCUMENT_URL=https://my408830-api.s4hana.cloud.sap/sap/opu/odata/sap/API_MATERIAL_DOCUMENT_SRV/A_MaterialDocument

# GCS / Storage
# If you set GOOGLE_APPLICATION_CREDENTIALS you may not need datecKeyCredentials
BUCKET_NAME=rpa_facturacion

# EasyContact / integration keys
EASYCONTACT_KEY=
ENVIRONMENT=

# Twilio (if used)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Other optional settings
# API keys used by CI/CD secrets (refer to GitHub Actions / Cloud Run secrets)
# datecKeyCredentials (or datecKeyCredentials_B64) may be set via your deployment secrets

# Security note: do NOT commit your .env file. Use Secret Manager or CI/CD secrets in production.
